<h1>STORES</h1>

<p style="text-align:center"><%= link_to "Back", products_path %></p>

<% @stores['result'].each do |store| %>
	<section>
		<p><%= link_to store['name'], store_path(store['id']) %> - <%= store['city'] %></p>
	
	</section>
<% end %>